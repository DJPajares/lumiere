{
  "name": "@lumiere/api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "echo \"api dev server pending\"",
    "build": "tsc --noEmit",
    "lint": "echo \"lint pending for api\"",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest --passWithNoTests"
  },
  "dependencies": {
    "@lumiere/config": "workspace:*"
  },
  "devDependencies": {}
}
