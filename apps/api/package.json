{
  "name": "@lumiere/api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "node --env-file=.env --watch --import tsx src/local-server.ts",
    "build": "tsc --noEmit && esbuild src/vercel.ts --bundle --platform=node --format=cjs --target=node20 --outfile=dist/vercel.cjs",
    "lint": "echo \"lint pending for api\"",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest --passWithNoTests"
  },
  "dependencies": {
    "@hono/node-server": "^2.0.10",
    "@lumiere/config": "workspace:*",
    "@lumiere/db": "workspace:*",
    "@lumiere/themes": "workspace:*",
    "@lumiere/types": "workspace:*",
    "exceljs": "^4.4.0",
    "hono": "^4.12.30"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "tsx": "^4.23.1"
  }
}
